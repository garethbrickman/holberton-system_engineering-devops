#!/usr/bin/env bash
### Display a string 10 times with if conditions on 4th, 8th loops
x="0"
while [  $x -lt 10  ]; do
    if [  $x == 3   ]; then
	echo 'bad luck'
    elif [ $x == 7  ]; then
	echo 'good luck'
    else
	echo 'Holberton School'
    fi
x=$((x+1))
done
